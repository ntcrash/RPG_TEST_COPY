# Megitech RPG — Automation Roadmap

Living backlog of work sized for autonomous/agent-driven execution. Generated 2026-07-07 from `README.md`, `CHANGELOG.md`, `HELP.md`, and a repo scan of `main.py` + `Code/`.

## How to work this list

- Gitflow: branch every item off `develop` as `feature/<slug>` (or `hotfix/<slug>` for P0 bugs), never commit directly to `main`.
- Version every merge: bump the version in `CHANGELOG.md` following the existing `MM/DD/YYYY - vX.Y.Z` convention and tag releases going forward
- Push every commit to `develop` per project convention.
- Check an item off by moving it to a `## Done` section with the commit hash and date, not by deleting it.

---

## P0 — Known bugs (carried from CHANGELOG.md ToDo)

## P1 — Repo hygiene (blocks safe automation)

_(all items complete — see Done; the pygame→Arcade migration closed at v2.2.5)_

## P2 — Testing & CI (enables safe automated changes)

_(all items complete — see Done)_

## P3 — Documentation debt

_(all items complete — see Done)_

## P4 — Feature ideas (lower priority, larger scope)

_(all items complete — see Done)_
---

## P5 - Visual Enhancements
30. [Feature] **World Aesthetics**: Cleaner grass-based terrain without flower clutter
31. [Feature] **Structured Paths**: Clear walking routes toward important areas
32. [Feature] **Add helper pets** Add companion pets that can assist in combat, you would have to earn from loot of a boss, or buy
33. [Feature] **Add more spells** add more spells for different classes and based on character level

## P6 - Multi-player Enhancements

40. [Release] **Change version number to v3.0.1 - Add multi player** add multi player with server / client setup, using the existing client and added the server components, single player mode should not change
41. [Feature] **Add account creation** Add account, and characters under the account for multi player, single player should not change


## In Progress

- **pygame → Arcade GUI migration** (MAJOR, v2.x) — started 07/07/2026 on `feature/v2-arcade`, foundation released as **v2.0.1**. Added `Code/version.py` (central `__version__`), `Code/gfx.py` (pygame→Arcade compat shim with automatic y-axis flip), and `arcade_app.py` (new `arcade.Window` entry point driving the existing state machine at 15 Hz via translated key/text events). Legacy `python main.py` (pygame) stays runnable throughout.
  - **v2.0.5 (07/07/2026)** — backend now switches end-to-end. **Key finding**: per-module `import pygame` swaps are a no-op because `Code/ui_components.py` star-exports its `pygame` binding to every module + `main.py`, so the star import always wins. Fixed the real mechanism instead: (1) `ui_components` selects the backend from `MEGITECH_BACKEND` (unset→pygame, `arcade`→`Code.gfx`) and star-exports it everywhere; (2) `arcade_app` sets the env var so its window renders through the shim; (3) `gfx` became a **hybrid** — non-render attrs (`mixer`/`time`/`display`/`event`/`key`/`sprite`/`locals`/…) delegate to real pygame via module `__getattr__`, and `Surface` now records+replays **off-screen** draws (HUD overlay + tile-sheet `area` blits), with y-flip and sub-rect clipping. Headless stub tests (5) pass; `python main.py` unchanged.
  - **v2.1.5 (07/09/2026)** — migrated the two modules that do NOT star-import `ui_components` (so the runtime switch could not reach them): `tile_map` and `animated_player`. Added `Code/backend.py` — a DRY single-source-of-truth switch (`from Code.backend import pygame`, same `MEGITECH_BACKEND` logic as `ui_components`) and pointed both modules at it. Extended `Code/gfx.py` to cover their sprite-sheet needs: `Surface.subsurface(rect)` (frame extraction) and texture-backed `blit(..., area)` now **crop** the sheet to the requested cell (`_crop_texture` via `Texture.crop`, PIL-crop fallback). Import-verified headlessly on-device under BOTH backends (`SDL_VIDEODRIVER=dummy` and `+MEGITECH_BACKEND=arcade`): 17/18 modules import clean in each mode.
  - **Per-module migration status** (import route → backend when `MEGITECH_BACKEND=arcade`):
    - ✅ via `ui_components` star-switch (no per-file change needed, confirmed no-op): `character_creation`, `combat_system`, `combat_integration`, `crafting_system`, `enhanced_combat_system`, `enhanced_combat_integration`, `inventory_system`, `level_system`, `rest_system`, `settings_system`, `store_system`.
    - ✅ via `Code.backend` switch (v2.1.5): `tile_map`, `animated_player`.
    - ✅ backend source itself: `ui_components` (v2.0.5).
    - n/a (no pygame): `game_data`, `enhanced_enemy_manager`, `debug`, `version`.
  - **v2.1.17 (07/10/2026)** — added `tests/test_gfx.py` (36 tests), the first direct coverage of the `Code/gfx.py` shim. Pins the arcade-independent core: pygame→Arcade Y-flip, color/rect/finite normalization, `_safe_text` emoji stripping, the full `Rect` API, off-screen `Surface` record/replay + `pygame.draw.*` op recording, `Font` metric fallback, and key/event constants. GPU-free via a `NoArcadeMixin` that forces `gfx._arcade = None`, so it runs deterministically with or without `arcade` installed. Full suite now **78/78**. This is the safety net for migration steps 1–2 below (both involve editing `gfx.py`).
  - **v2.1.18 (07/10/2026)** — fixed a real visual bug found while auditing the shim for step 2: `Surface.set_alpha()` was ignored when an off-screen surface's recorded fill/shape ops were replayed, so the semi-transparent dimmer overlays in the crafting (alpha 180) and inventory/store (alpha 200) screens painted **solid black** over the whole game world instead of dimming it. Added `_apply_alpha(color, alpha)` to `Code/gfx.py` and threaded it through every shape op in `Surface._replay` (fill/rect/circle/ellipse/line/lines/polygon); nested blits already carried alpha. Added 2 unit tests to `tests/test_gfx.py`; suite now 78→80 green, `py_compile`/ruff clean.
  - **v2.1.19 (07/10/2026)** — fixed a latent API-compat bug in the shim's `Font` found while auditing for step 2: `Font.__init__` stored the point size as `self.size`, shadowing the pygame-standard `size(text) -> (w, h)` measuring **method** with an int, so `font.size("text")` would raise `'int' object is not callable`. The game measures text via `render(...).get_width()` today so it wasn't live-crashing, but it broke the "faithful `import pygame` replacement" contract. Renamed the stored size to `self.font_size` throughout `Font` (freeing the method); grep-verified nothing reads `font.size` as an attribute. Added 2 unit tests; suite now 80→82 green, `py_compile`/ruff clean.
  - **v2.1.20 (07/10/2026)** — fixed a live crash found while auditing the shim's `Font` for step 2: `Code/inventory_system.py` calls `self.font.get_height()` (to size a selection highlight), which real `pygame.font.Font` provides but the `gfx` shim `Font` did not — so under `MEGITECH_BACKEND=arcade` it raised an uncaught `AttributeError` (the call is outside that code's `try/except`) and aborted the inventory/store draw. Added `get_height()` to the shim `Font` (renders an ascender+descender sample `"Ay"`, returns its pixel height; no-arcade fallback returns the point size) plus companion `get_linesize()`/`get_ascent()`/`get_descent()` for a faithful drop-in. Added 3 unit tests; full suite now 82→**84** green, `py_compile` clean.
  - **v2.1.21 (07/10/2026)** — **migration step 1 (on-device visual verification) is DONE and automated.** Discovered the sandbox *can* get a GL context via `xvfb` (software/llvmpipe), so the previously human-only visual pass is now scripted. Added `tests/render_smoke.py` (drives `arcade_app.MagitechWindow` → the real `EnhancedGameManager` through all 13 `GameState`s, rendering each to a PNG and asserting no crash + non-black frame) and `tests/test_render_smoke.py` (subprocess wrapper; auto-uses `xvfb-run`, skips cleanly when arcade/display absent). **Result: 13/13 screens render crash-free** under `MEGITECH_BACKEND=arcade`; visually spot-checked menu/game-board/HUD (the v2.1.18 alpha fix dims correctly). No new shim primitives needed. Full suite **84→85** green.
  - **v2.2.0 (07/10/2026)** — **migration step 4 (flip the default) is DONE.** `python main.py` now launches the Arcade window: `main.py` sets `MEGITECH_BACKEND=arcade` (+ headless `SDL_VIDEODRIVER=dummy`) via `setdefault` *before* any `Code.*` import so the `ui_components` star-export binds the shim, then its `__main__` block delegates to `arcade_app.main()`. The legacy pygame `run()` loop is DEPRECATED but retained one release as an opt-in fallback (`MEGITECH_BACKEND=pygame python main.py`). `Code/version.py` `BACKEND` flipped `"pygame"`→`"arcade"`. Verified: suite **85/85**; default no-env import binds `main.pygame == Code.gfx` (+dummy SDL); `MEGITECH_BACKEND=pygame` binds real pygame; `render_smoke` 13/13; `python main.py` launched the Arcade window headlessly (`xvfb`) crash-free for 6s (only ALSA no-audio noise).
  - **v2.2.1 (07/10/2026)** — **migration step 5 (remove the deprecated pygame loop) is DONE.** Deleted `EnhancedGameManager.run()` (the ~40-line hand-rolled pygame `while running:` loop) and the `MEGITECH_BACKEND=pygame` fallback branch in `main.py.__main__`; `python main.py` now unconditionally delegates to `arcade_app.main()`. Removed the orphaned `import sys`. `MEGITECH_BACKEND` now only selects the drawing binding (shim vs. real pygame) star-exported by `ui_components`, still used by the headless test suite. Verified: suite **85/85** (render_smoke 13/13), `python main.py` opens the Arcade window crash-free under `xvfb`. **Key finding:** `pygame` can NOT yet be removed from `requirements.txt` — `Code/gfx.py` delegates audio (`pygame.mixer`), timing, and input constants to real pygame. Retiring that dependency is now tracked as step 6.
  - **Remaining to fully retire pygame**:
    1. ✅ **On-device VISUAL verification (v2.1.21, 07/10/2026)** — all 13 screens confirmed painting through `gfx` under `MEGITECH_BACKEND=arcade`, crash-free, via `tests/render_smoke.py` (repeatable, `xvfb`-driven). This unblocked step 4.
    2. ✅ **Extend the shim** — no primitive rendered wrong during (1); the shim as of v2.1.20 (Y-flip, alpha overlays via `_apply_alpha`, tile-sheet crop, Font metrics) covers every screen. Re-open only if step-4 regressions surface.
    3. ✅ **Fixed pre-existing bug** (v2.1.9, 07/09/2026): `Code/combat_integration.py` bare `from game_data import CharacterManager` → `from Code.game_data import CharacterManager`. Full `Code/*.py` import sweep now 20/20 clean under both backends (was 19/20).
    4. ✅ **Flip the default (v2.2.0, 07/10/2026)** — `python main.py` now defaults to Arcade (delegates to `arcade_app.main()`); legacy pygame loop is opt-in via `MEGITECH_BACKEND=pygame`.
    5. ✅ **Removed the deprecated pygame `run()` loop (v2.2.1, 07/10/2026)** — deleted `EnhancedGameManager.run()` and the `MEGITECH_BACKEND=pygame` fallback branch in `main.py.__main__`; `python main.py` now unconditionally delegates to `arcade_app.main()`. Also removed the now-unused `import sys`. Verified: suite 85/85 (incl. render_smoke 13/13), `python main.py` launches the Arcade window crash-free under `xvfb`. **Finding:** the `pygame` runtime dep could NOT be dropped — `Code/gfx.py` still delegates audio (`pygame.mixer`), timing, and input constants to real pygame. That work is split out as step 6 below.
    6. **Port audio/timing/input off pygame, then drop the `pygame` dep (closes P1 #1)** — replace the `Code/gfx.py` `__getattr__` delegation to real pygame with Arcade-native equivalents. Progress:
       - ✅ **Audio + timing (v2.2.2, 07/10/2026)** — `Code/gfx.py` now defines native `mixer` (`arcade.Sound`-backed `Sound`/`music`, defensive no-op when no audio device), `time` (stdlib-clock `wait`/`Clock`/`get_ticks`), and an `error` exception class, so SFX/music (`Code/enhanced_combat_system.py`) and `pygame.time.wait` no longer route through real pygame. Input **constants** (`K_*`, event types) were already shim-native. +17 tests (`AudioTimingShimTests`); suite 98/98; verified against real `arcade.Sound` under `xvfb` (20/20 WAVs load, play/stop crash-free).
       - ✅ **Sprite + locals (v2.2.3, 07/10/2026)** — `Code/tile_map.py` and `Code/animated_player.py` were the last two modules importing real pygame directly (they bypass the `ui_components`/`backend` drawing switch for `from pygame.locals import *` and for subclassing `pygame.sprite.Sprite`). Added a native `Sprite` class + `sprite` namespace to `Code/gfx.py` (module-level, so it wins over `__getattr__`); removed the dead/`locals` star-imports and qualified `animated_player`'s `Rect`/`K_*` uses as `pygame.Rect`/`pygame.K_*` (resolve to the shim under Arcade, real pygame under the pygame backend). Both modules now import clean under both backends and carry `gfx.Sprite` in their MRO under Arcade. +4 tests (`SpriteTests`); suite 98→102 green under both backends.
       - ✅ **Display + event + init (v2.2.4, 07/10/2026)** — ported the last runtime call sites that fell through the shim's `__getattr__` to real pygame: `pygame.display.set_mode`/`set_caption` (`main.py.__init__`) and `pygame.event.Event(...)` (crafting input). Added native `display` (`set_mode` → off-screen `gfx.Surface`; `set_caption`/`get_caption` in-memory; lifecycle no-ops) and `event` (`Event` aliased to the native class; queue ops no-ops) modules to `Code/gfx.py`, plus a `NOEVENT` constant, and made `init()`/`quit()` native (no real-pygame delegation). **As of v2.2.4 nothing the game exercises at runtime routes through real pygame under the Arcade backend** — only unused extras (`locals`/`Color`/`Vector2`/`mouse`) still delegate. +7 tests (`DisplayEventShimTests`); suite 102→109 green under both backends; verified with `gfx._pygame = None` that display/event/init use no real pygame.
       - ✅ **Dropped the `pygame` dependency (v2.2.5, 07/10/2026) — CLOSES P1 #1.** Removed `pygame>=2.5` from `requirements.txt`; Arcade is now the sole runtime dep. Simplified the backend switch to a single source of truth: `Code/backend.py` now defaults to the `Code.gfx` shim (unset/empty/`arcade` → shim; `MEGITECH_BACKEND=pygame` → real pygame, an explicit opt-in requiring a self-installed pygame). `Code/ui_components.py` now imports from `Code.backend` (dropped its inline duplicate switch + unused `import os`); the 11 gameplay modules that had a bare redundant `import pygame` now import from `Code.backend` too, so nothing hard-crashes when pygame is absent. Reworked the test suite (`tests/__init__.py` + the 3 hard-importing modules) to default onto the shim and init via `Code.backend`. Verified with **pygame uninstalled**: 20/20 module import sweep clean, full suite 109/109 (1 skipped render_smoke), `py_compile` clean, backend matrix correct. The shim keeps a lazy guarded `import pygame` solely so the opt-in backend works for devs who install it.

## Done

- **Special Victory Messages** (P5 #29) — done 07/11/2026 on `feature/victory-messages`, released as **v2.8.0**. Boss defeats now show a multi-line trophy banner instead of one flat line, and normal wins a compact one. **Key finding**: the old victory strings used astral-plane emoji (🏆/🎉/⭐, all > U+FFFF) which the `Code.gfx` renderer silently strips, so the "trophy" never actually appeared on-screen under the Arcade backend. Added a pure/testable module-level `build_victory_messages(is_boss, xp_gained, credits_gained, leveled_up)` in `Code/enhanced_combat_integration.py` that returns `(text, color)` lines built from **BMP** glyphs (★ U+2605, ✦ U+2726, ═ U+2550) that survive the renderer — a `★ ═══ ★` border framing `★ 🏆 BOSS DEFEATED! 🏆 ★` (GOLD) + `Level Complete!` (YELLOW) + `+XP / +Credits` reward (GREEN) for bosses, `✦ Victory! ✦` + reward for normal wins, and a highlighted `★ LEVEL UP! ★` line when leveling. The 🏆 emoji is kept in the text so it still shows under the real-pygame backend / in logs. `handle_victory` now logs each banner line separately. +10 tests (`tests/test_victory_messages.py`, headless): banner shape, reward/level-up/colour coding, determinism, ≤60-char log width, and a BMP-only guard proving the decorations render. `py_compile` + Ruff clean on the new code; suite 194→**204** green. See CHANGELOG.md.
- **"BOSS DUNGEON" golden glow** (P5 #28) — done 07/11/2026 on `feature/boss-dungeon-glow`, released as **v2.7.0**. Replaced the flat static 4-corner drop-shadow on the dungeon-entrance label (`Dungeon.draw`, `Code/ui_components.py`) with animated mystical signage: a pulsing radiant aura (`label_glow_offsets` — 3 concentric rings × 8 compass directions, blending dark `label_glow`→bright `label_color`, breathing radius), a shimmer (`label_shimmer`, slow sine + fast flicker driving text + aura brightness), and a ±2px hover (`label_bob`). Runs off the existing `portal_theme` palette so "BOSS DUNGEON" glows gold and "LEVEL PORTAL" glows teal from one code path. All helpers pure/deterministic in `animation_timer`. +7 tests (`LabelGlowTests` in `tests/test_dungeon_portal.py`, headless via the `gfx` shim); `py_compile` + Ruff clean; suite 187→194 green. See CHANGELOG.md.
- **Mystical Dungeons** (P5 #27) — done 07/11/2026 on `feature/mystical-dungeons`, released as **v2.6.0**. Rebuilt `Dungeon.draw` in `Code/ui_components.py` from three flat circles + a static orbiting particle ring into a fully animated mystical gateway: a five-ring pulsing glow halo (`glow_pulse()` + `_lerp_color`), a three-arm swirling vortex spiralling into a bright pulsing core (`swirl_point()`), and six rising, twinkling motes (`particle_state()`). Added `portal_theme(is_portal)` so the post-boss **level portal** (`is_portal=True`) now reads as a cool teal/cyan **"LEVEL PORTAL"** distinct from the violet gold-labelled **"BOSS DUNGEON"** entrance (previously both drew an identical boss entrance). All effect helpers are pure/deterministic in `animation_timer` for testing. +19 tests (`tests/test_dungeon_portal.py`, headless via the `gfx` shim); `py_compile` + Ruff clean; suite 168→187 green. See CHANGELOG.md.
- **Resource-node visual feedback** (P5 #26) — done 07/10/2026 on `feature/resource-node-feedback`, released as **v2.5.0**. Extended the v2.4.0 tree harvest-feedback pattern to the other four gathering nodes (Rock, Metal, Stream, Brush) in `Code/ui_components.py`. Added a shared `HarvestableNode` mixin (all four now subclass it) centralising `regrow_fraction()` (0→1 over the respawn timer), a `_lerp_color` helper, `harvest_pulse()`, and `draw_ready_marker()` (a pulsing sparkle drawn above a node ONLY when harvestable). Each node's `draw` now tweens from a dark depleted look back to full colour as it regrows (rock/metal refill ore colour + rock shrinks while spent; stream brightens murky→clear blue; brush greens up + foliage radius scales with regrowth) instead of the old binary colour swap. Also removed a pre-existing dead `flow` local in `Stream.draw`. +13 tests (`tests/test_resource_node_feedback.py`, headless via the `gfx` shim); `py_compile` + Ruff clean; suite 155→168 green. See CHANGELOG.md.
- **Animated Trees** (P5 #25) — done 07/10/2026 on `feature/animated-trees`, released as **v2.4.0**. Reworked `Tree.draw` in `Code/ui_components.py`: canopies now sway with a primary breeze + slower gust + vertical bob (per-tree `sway_phase`/`sway_strength` so a forest isn't in lock-step; trunk stays rooted), leafy trees use a fuller multi-blob canopy with outline + sun-catch highlight, and pine layers lean with the wind. Harvest state now drives colour AND size via `regrow_fraction()` (0→1 over the respawn timer) + a `_lerp_color` helper: a depleted tree starts small/withered grey-brown and tweens back to healthy green at full size as it regrows, and sway amplitude scales with growth. +11 tests (`tests/test_tree_animation.py`, headless via the `gfx` shim); suite 144→155 green. See CHANGELOG.md.
- **Stop real pygame loading on every launch** (startup / repo hygiene, not a numbered item) — done 07/10/2026 on `hotfix/lazy-pygame-import`, released as **v2.3.4**. `Code/gfx.py` imported real `pygame` eagerly at module load to back a handful of never-exercised `__getattr__` fallbacks (`locals`/`Color`/`Vector2`/`mouse`/…); since the migration completed (v2.2.5) nothing the game runs hits those, so the eager import only paid pygame's SDL load + banner cost on every Arcade launch and silently swallowed an `ImportError` on clean installs (pygame is no longer a declared dep). Replaced it with a lazy, cached, banner-silenced `_get_pygame()` (sets `PYGAME_HIDE_SUPPORT_PROMPT=1`, `_pygame_tried` guards retry) wired into both `__getattr__` hooks — a normal play session now never imports pygame. Also gitignored `*.log`/`importtime.log` and removed a stray `importtime.log` profiling artifact that had been accidentally git-added. `py_compile` clean; suite **144/144** (66 `test_gfx` shim tests green with pygame absent). See CHANGELOG.md.
- **Fix Menu items overlapping** (P5 #34) — done 07/10/2026 on `hotfix/menu-overlap`, released as **v2.3.3**. The combat, spell, item, and store sub-menus were drawn at fixed y-coordinates sized for shorter lists and collided with the panels below them. Fixes: (combat) lowered the combat-log background + text from y=390/400 to y=455/463 so the action/spell/item sub-menus above have room to grow; (spell menu) moved the dynamically-sized spell box from y=210 to y=190 so its worst case (3 spells → 215px) and instruction bar clear the log at y=455; (item menu) started the potion list at y=190 with an explicit 30px `row_pitch` (was 35px, overran on long potion lists) and tightened the selection highlight to 26px; (store) capped `EnhancedStoreManager.items_per_page` from 12 → 9 so the item rows (30px pitch from y=150) end ~y=420 instead of running to y=510 into the control-instruction bar, with PgUp/PgDn scrolling the rest; (arcade overlay) the migration-era dev banner/state-readout/footer-hint drew on top of the live combat HUD now that migration is complete — moved behind `MEGITECH_DEBUG` (off by default). `py_compile` clean; full suite **144/144** green (6 skipped when arcade/display absent). See CHANGELOG.md.
- **Fix window close** (P5 #24) — done 07/10/2026 on `feature/window-close-fix`, released as **v2.3.2**. The window "X" button, the Quit menu item, and Escape now all end the whole program through one idempotent `_shutdown()` in `arcade_app.py`. Removed the double-close (`on_close()` used to call `_shutdown()` **and** `super().on_close()`, closing the already-dead window a second time); `on_close()` now just delegates to `_shutdown()`. `_shutdown()` guards re-entry via `self._closing`, saves progression once (failure-tolerant), calls `self.close()`, then `arcade.exit()` so `arcade.run()` returns; `main()` then `os._exit(0)`s to guarantee no lingering background (audio) thread keeps the interpreter alive after the window closes. +5 arcade-guarded tests (`tests/test_window_close.py`, GPU-free via `object.__new__`) pinning save-close-exit, idempotency, save-failure tolerance, missing-`level_manager`, and the on_close→_shutdown route. `py_compile` clean; full suite **139 → 144** green (6 skipped when arcade absent). See CHANGELOG.md.
- **Difficulty/balance regression checks** (P4 #15) — done 07/10/2026 on `feature/balance-regression`, released as **v2.3.1**. Added `tests/test_balance_regression.py` (16 tests) pinning the exact numeric output of the balance-critical formulas that were repeatedly hotfixed in the past (difficulty multiplier / stat scaling, v1.4.1 & v1.7.3): enemy HP scaling `(hp_base + (level-1)*12 + variance) * difficulty_multiplier` across normal/hard(2.0)/easy(0.5) with the `int()` truncation + `max(1,…)` floor and variance-before-multiplier ordering; the book-level→tier boundaries (basic ≤3 / elite 4–6 / champion 7–10 / ancient 11–15 / boss 16+); boss scaling (steeper `*30` HP curve, `(book_level+3)*mult` level); the `[0.1, 3.0]` `set_difficulty_multiplier` clamp; and armor class `10 + dex_bonus + best_armor` — explicitly guarding that multiple equipped armors take the **max** bonus, not the sum. All randomness pinned via `mock.patch` so each expected value is exact. `py_compile` clean; full suite **123 → 139** green (1 skipped: display-dependent render_smoke). Complements `tests/test_combat_system.py` (pure CombatManager math). See CHANGELOG.md.
- **Automated save-file migration check** (P4 #14) — done 07/10/2026 on `feature/save-file-migration`, released as **v2.3.0**. Added a versioned save schema (`Save_Version`) and a dependency-free migration/validation pass (`Code/save_migration.py`): `migrate_character()` backfills every required field with a sensible default (mirroring `create_sample_character`), coerces the numeric fields to `int` and clamps the non-negative ones, guarantees `Inventory` is a dict and all six ability scores exist, and stamps `SAVE_SCHEMA_VERSION` — healing old/partial saves so the game's many direct-index reads (`character_data["Aspect1_Mana"]` etc.) can't `KeyError` mid-combat. Conservative (only adds/fixes, never deletes unknown keys) and idempotent. `CharacterManager.load_character()` runs it on every load and re-writes the healed file when anything changed; `create_sample_character()` stamps the version. +14 tests (`tests/test_save_migration.py`) incl. a load→migrate→re-save integration test; suite 109→123 green, `py_compile`/ruff clean. Establishes `Save_Version` as the anchor for future save-format changes. See CHANGELOG.md.
- **pygame→Arcade migration COMPLETE / drop the `pygame` dependency** (P1 #1) — done 07/10/2026 on `feature/port-audio-timing`, released as **v2.2.5**. Final step of the multi-release migration begun at v2.0.1. Removed `pygame>=2.5` from `requirements.txt` (Arcade is now the sole runtime dependency), inverted `Code/backend.py` to default to the Arcade-native `Code.gfx` shim (real pygame is an explicit `MEGITECH_BACKEND=pygame` opt-in), routed `Code/ui_components.py` + all 11 gameplay modules' `pygame` name through the single `Code.backend` switch, and reworked the test suite to run on the shim with no pygame installed. Verified with pygame uninstalled: 20/20 import sweep, suite 109/109 (1 skipped), `py_compile` clean. See CHANGELOG.md.

- **Reconcile version history** (P3 #12) — done 07/10/2026 on `feature/arcade-migrate-assets`, released as v2.1.16. Confirmed via repo scan that the legacy multiplayer prototype documented in the CHANGELOG's early history (v23–v29, Aug 2025) — `NetworkManager_Class.py`, `Server_v1`–`v3`, `config.py`, `classes.py`, HTTP-on-8080 server — has **no surviving code** in the current single-player tree (no network/socket imports anywhere; only two orphaned SQLite DBs remain in `assets/`). Rather than delete the history, archived it: added a `⚠️ LEGACY / DISCONTINUED PROTOTYPE` banner above the v29 section marking it discontinued and cross-referencing the *fresh* future multiplayer plan (P6 #40/#41, v3.0.1), which builds a new server/client layer and shares none of the prototype's code. Verdict for future planning: multiplayer is a live future goal but must be planned from scratch — no existing server component to build on. Docs-only; `py_compile` clean, tests 42/42. See CHANGELOG.md.
- **Add `requirements.txt`** (P1 #7) — done 07/10/2026 on `feature/arcade-migrate-assets`, released as v2.1.15. Reconciliation: the manifest already existed (added during the v2.x migration) and fully satisfies the item. Verified via a full import sweep of `main.py`, `arcade_app.py`, and `Code/*.py` that the only third-party runtime deps are `pygame` and `arcade`, both pinned in `requirements.txt` (`pygame>=2.5`, `arcade>=3.3`); dev-only `ruff` is in `requirements-dev.txt`. The lower-bound pins are intentional for the migration window (both backends installable together). No dependency drift — nothing to add/remove. Docs/metadata-only. See CHANGELOG.md.
- **Sync `README.md` architecture section with actual code** (P3 #11) — done 07/10/2026 on `feature/arcade-migrate-assets`, released as v2.1.14. The README's "Project Architecture" was frozen at the pre-v1.6 layout: it named `game_states.py` as the engine/entry point (renamed to `main.py` in v1.6) and listed every module as a flat root-level file, though all game code has long lived under the `Code/` package. Rewrote the entry-point (`main.py` default / `arcade_app.py` Arcade), dependency (pygame ≥ 2.5 + arcade ≥ 3.3), backend-switch (`MEGITECH_BACKEND`, `Code/backend.py`, `Code/gfx.py`), file-structure (full `Code/` tree + `tests/` + gitignored save dirs + tooling files), and run-instructions sections to match the real code. Docs-only; `py_compile` clean, tests 42/42. See CHANGELOG.md.
- **Add a CI workflow** (P2 #9) — done 07/09/2026 on `feature/arcade-migrate-assets`, released as v2.1.13. Added the repo's first GitHub Actions workflow (`.github/workflows/ci.yml`) that runs on every push and PR against `main` and `develop` (plus manual `workflow_dispatch`). Steps: checkout → set up Python 3.11 (pip-cached) → install `requirements.txt` + `requirements-dev.txt` → `python -m py_compile main.py arcade_app.py Code/*.py` → `ruff check .` → headless `python -m unittest discover -s tests -v`. `SDL_VIDEODRIVER`/`SDL_AUDIODRIVER` are forced to `dummy` at job scope so pygame imports without a display/audio device; a `concurrency` group cancels superseded runs. The ruff step is intentionally `continue-on-error` for now — the 43 pre-existing findings are deferred in the CHANGELOG ToDo — and flips to blocking once that cleanup lands. Locally verified before commit: workflow YAML parses, `py_compile` clean, ruff reports the expected 43 findings, and the suite is 42/42 green under the dummy SDL drivers. See CHANGELOG.md.
- **Fix spawn items so they are always accessible** (P1 #3) — done 07/09/2026 on `hotfix/spawn-accessible`, released as v2.1.4. World items (trees, rocks, metal veins, streams, brushes) were placed at random coordinates constrained only by min-distance to other spawned entities, with x/y ranges hardcoded to the old 800×600 window (world is really 768×576) and no clearance around the player start or key interactables — so a blocking rock/metal/tree could land on the player's spawn point (trapping them) or bury the rest area / shop / boss-dungeon centre. Added `get_spawn_bounds(margin)` (derives spawn ranges from `tile_map.get_world_pixel_size()`, clamped so they never invert) and `get_reserved_positions()` (player start, rest area, shop, dungeon centre); both spawn loops now reject candidates outside the derived bounds, on a non-walkable tile (`is_position_walkable`), or within `RESERVED_CLEARANCE = 70`px of any reserved point. New `tests/test_spawn_accessibility.py` (6 tests) drives the real placement loops across 60 seeds asserting the invariants; suite now 42/42 green. See CHANGELOG.md.
- **Replace 0-byte placeholder SFX** (P4 #17) — done 07/09/2026 on `feature/placeholder-sfx`, released as v2.1.3. Generated six real WAV effects to replace the empty 0-byte files that failed to load (`player_hurt`, `run_away`, `victory`, `menu_select`, `menu_move`, `door_open`). Synthesized procedurally with NumPy (44.1 kHz, 16-bit stereo, short envelopes): `menu_move` a 60 ms soft blip, `menu_select` a two-note up-confirm, `player_hurt` a descending tonal+noise grunt, `run_away` a rising whoosh sweep, `victory` a four-note C-E-G-C major arpeggio fanfare, and `door_open` a low modulated creak. All six load through `pygame.mixer.Sound` headlessly (dummy SDL drivers) with correct non-zero durations. The generator script logic is captured in CHANGELOG. See CHANGELOG.md.
- **Add a linter config** (P2 #10) — done 07/08/2026 on `feature/linter-config`, released as v2.1.2. Added the project's first `pyproject.toml` with a `[tool.ruff]` section: `py311`, `line-length = 120`, content/vendored dirs excluded, and a high-signal `select = ["F", "E9", "B"]` rule set (dead code, unused imports, syntax errors, bugbear) with style rules left off so it's not a reformat diff. Key finding: the game's deliberate `from Code.ui_components import *` backend switch (v2.0.5) makes `F403`/`F405` fire on nearly every pygame call — **484 of 527 raw findings**; ignoring them leaves **43 real findings** (13 unused imports, 12 unused vars, 13 unused loop vars, 4 placeholder-less f-strings, 1 redefinition), now logged in CHANGELOG ToDo for a dedicated cleanup pass (17 are `--fix`-safe). Added `requirements-dev.txt` for the dev-only `ruff` dep. Tests still 36/36 green; `py_compile` clean. See CHANGELOG.md.
- **Add a minimal test suite** (P2 #8) — done 07/08/2026 on `feature/test-suite`, released as v2.1.1. Added a `tests/` package (standard-library `unittest`, no third-party runner) that forces SDL dummy drivers so the Pygame-coupled modules import headlessly. Three modules — `test_combat_system.py` (damage/spell/hit-chance math + spell gating, randomness pinned via `mock.patch`), `test_level_system.py` (unlock/selection/completion cascades + content-count helpers, progression written to a temp CWD), and `test_crafting_system.py` (recipe/material catalog, level-gated availability, rarity colours, random drop table). **36 tests, all green**; `py_compile` clean. Run with `python -m unittest discover -s tests -v`. See CHANGELOG.md. (Surfaced a leftover unconditional DEBUG print at `Code/combat_system.py:266` — noted for a future run.)
- **Character spawns into last level played** (P1 #4) — done 07/08/2026 on `feature/spawn-last-level`, released as v2.1.0. `LevelManager` already persists `current_world`/`current_level` per character (`SaveProgression/progression_<name>.json`) and restores it on `set_character()`, but the character-select and character-create paths jumped straight to `GAME_BOARD` without rebuilding the world, so players always spawned into the default 1-1 world built at startup. Added `enter_game_board_for_current_level()` (rebuilds the world for the persisted level, recenters the player, snaps the camera) and called it from both the load-character and create-character paths. Explicit level selection is unchanged (still routes through `change_level`). See CHANGELOG.md.
- **Save-file tracking policy** (P1 #5) — done 07/08/2026 on `hotfix/save-file-policy`, released as v2.0.6. Decided runtime saves are live per-player state, not versioned source: gitignored `Characters/*.json` + `SaveProgression/*.json`, untracked the five previously committed saves via `git rm --cached`, and added `.gitkeep` to both dirs so a fresh clone keeps the folders. Loader already discovers characters by scanning the dir, so nothing depends on a committed save. See CHANGELOG.md.
- **Resource regeneration too fast** (P4 #16) — done 07/07/2026 on `hotfix/resource-regen-rate`, released as v2.0.4. Respawn timers were minutes×60 (seconds) but the loop runs at 15 FPS, so nodes regrew 15× too fast (~40–80s vs. intended 10–20 min). Corrected `max_respawn_time` in `Code/ui_components.py` (Tree/Stream/Rock/Metal/Brush) and `Code/crafting_system.py` (ResourceNode) to true 15-FPS frame counts. See CHANGELOG.md.
- **Remove/gate debug output** (P1 #6) — done 07/07/2026 on `feature/gate-debug-output`, released as v1.7.8. Added `Code/debug.py` with a DEBUG flag (off by default, opt-in via `MEGITECH_DEBUG` env var) and a `debug_print()` helper; replaced all 19 unconditional `print(f"DEBUG: ...")` calls in `Code/combat_system.py` (14) and `Code/settings_system.py` (5). See CHANGELOG.md.
- **Main game sound is broken** (P0 #2) — done 07/07/2026 on `hotfix/audio-path-fix`, released as v1.7.7. Fixed `../Sounds` vs `Sounds` path mismatch, made music resolve against the absolute Sounds dir regardless of working directory, and defined the missing `start_world_music()` hook. See CHANGELOG.md.
- **Dying doesn't take credits** (P0 #1) — done 07/07/2026 on `hotfix/death-credit-loss`, released as v1.7.6. Fixed legacy combat death path, boss-dungeon FIGHT-state fallthrough, keypress-path reward/penalty skip, and duplicated victory rewards. See CHANGELOG.md.
- **Add a `.gitignore`** + **Untrack committed `.pyc` files** (P1 #3 & #4) — done 07/07/2026 on `feature/repo-hygiene`, released as v1.7.5. See CHANGELOG.md.

_(move completed items to CHANGELOG.md)
